import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Geliştirme rozetini gizle: demo ve ekran görüntülerinde köşede "N" logosu görünmesin.
  devIndicators: false,
};

export default nextConfig;
